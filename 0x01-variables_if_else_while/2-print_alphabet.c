#include <stdio.h>
/**
 * main - Prints alphabet in lowecase
 *
 * return: Always 0
 */

int main(void)
{
	char a;

	for (a = 'c'; a <= 'z'; a++)
		{
			putchar(a);
		}
	putchar('\n');
	return (0);
}
